import { Settings } from '../types/settings';

export const defaultSettings: Settings = {
  type: 'css',
  onlyUnique: true,
  resultsToDisplay: 3,
  scoreTolerance: -50,
  gibberishTolerance: 0.0728,
};
